# roztomile (project mirror)

Mirrors **project-owned** slash-command prompts from `aidd-custom/commands/` into `.cursor/commands/roztomile/` for Cursor. Canonical source stays under `aidd-custom/`; run **`/sync-aidd-custom`** to refresh.

## Files

### /sync-aidd-custom

**File:** `sync-aidd-custom.md` (mirror of `aidd-custom/commands/`; Cursor may load `.cursor/commands/sync-aidd-custom.md` at repo root for `/sync-aidd-custom`)

Invokes the sync workflow; implementation lives in `aidd-custom/skills/sync-aidd-custom/SKILL.md`.
