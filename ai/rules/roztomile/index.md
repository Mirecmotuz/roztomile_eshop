# rules

This index provides an overview of the contents in this directory.

*No project-owned rule files yet. Add `.md` or `.mdc` here as the canonical source; mirror to `.cursor/rules/<repo-root>/` via `/sync-aidd-custom`.*
