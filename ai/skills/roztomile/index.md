# roztomile (project mirror)

Mirrors **project-owned** Agent Skills from `aidd-custom/skills/` into `.cursor/skills/roztomile/` for Cursor. Canonical source stays under `aidd-custom/`; run **`/sync-aidd-custom`** to refresh.

## Subdirectories

### 📁 sync-aidd-custom/

See [`sync-aidd-custom/SKILL.md`](./sync-aidd-custom/SKILL.md) — sync `aidd-custom/` → `.cursor/<repo-root>/` (diff-first, incremental).
