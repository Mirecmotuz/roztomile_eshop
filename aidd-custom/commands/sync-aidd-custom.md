## /sync-aidd-custom

Run the **sync-aidd-custom** workflow exactly as defined in the skill:

**Read and follow:** `.cursor/skills/roztomile/sync-aidd-custom/SKILL.md` (Cursor mirror for this repo).

*Canonical source to edit:* `aidd-custom/skills/sync-aidd-custom/SKILL.md` — after changes, re-run this command so `.cursor/` stays in sync.

Constraints {
Before starting, read the full `SKILL.md` and obey all steps and guardrails (diff-first, incremental writes, index rules, CLAUDE.md updates).
Do not skip Step 2 (inventory/diff).
}
